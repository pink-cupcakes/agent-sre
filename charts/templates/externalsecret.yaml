# The agent-sre-secret is created/updated by the GitHub Actions deploy workflow
# using the ANTHROPIC_API_KEY GitHub repo secret. No manual secret management needed.
#
# To create it locally for development:
#   kubectl create secret generic agent-sre-secret \
#     --namespace agent-sre-dev \
#     --from-literal=ANTHROPIC_API_KEY=<your-key>
